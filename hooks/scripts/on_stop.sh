#!/usr/bin/env bash
# DEPRECATED — this script is no longer used.
# Skill detection is now handled by on_skill_use.sh via PostToolUse+Skill matcher,
# which correctly receives tool_input. Stop hook does not provide tool_input.
exit 0
